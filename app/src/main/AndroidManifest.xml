<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- QR scanner uses the camera. android.hardware.camera.any with required=false
         keeps the app installable on devices without a back camera (rare). -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- الفروع: ترتيب بالأقرب للمستخدم.
         COARSE كفاية تماماً للغرض ده (دقة ~١ كم) وبتدي المستخدم خيار
         "الموقع التقريبي" في نافذة الإذن. FINE اختيارية للي يوافق
         عليها وبتدي ترتيب أدق للفروع المتقاربة.
         الاتنين اختياريين — الشاشة شغالة من غيرهم بترتيب اللوحة. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- من أندرويد 11، التطبيق مش بيشوف التطبيقات التانية إلا لو
         أعلن عنها. من غير البلوك ده `resolveActivity` بترجع null
         وأزرار «الاتجاهات» و«اتصال» و«واتساب» تبان مقفولة غلط. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
    </queries>

    <application
        android:name="com.msa.android.MSAApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MSA.Splash"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="36">

        <property

            android:name="android.adservices.AD_SERVICES_CONFIG"

            android:resource="@xml/gma_ad_services_config"

            tools:replace="android:resource" />

        <!--
          AdMob — APPLICATION_ID is REQUIRED. Without this tag the SDK calls
          System.exit() on startup. The value below is Google's official sample
          test app ID — serves test ads only, never billed, safe to ship for
          development. Replace with your real App ID from admob.google.com once
          you've set up your app there. Format: ca-app-pub-XXXXX~YYYY (tilde).
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name="com.msa.android.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|layoutDirection|locale"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MSA.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FCM Service -->
        <service
            android:name="com.msa.android.presentation.service.MSAFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_gold" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="msa_channel_default" />

        <!-- مفتاح Google Maps — بيتحقن وقت البناء من local.properties
             (MAPS_API_KEY). المفتاح عمره ما يتحط في الكود المصدري. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

    </application>

</manifest>
